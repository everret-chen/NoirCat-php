includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6
    paths:
        - app

    # Keep the analysis cache inside the project instead of the system temp dir.
    tmpDir: .tmp/phpstan
